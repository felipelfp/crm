{
  "name": "luvi-crm-monolith",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix React && npm run build --prefix React",
    "start": "node server/index.js"
  }
}
